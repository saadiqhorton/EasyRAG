---
description: Update spec.md and supporting docs to reflect a newly agreed architecture, workflow, or requirement change.
---

Update the project spec for: $ARGUMENTS

Rules:
1. Identify which file should change: `spec.md` or a specific file in `docs/`.
2. Keep `spec.md` high level and move detail into the appropriate supporting doc.
3. Avoid changing implementation without updating the docs when drift is intentional.
4. Preserve consistency across linked docs.
