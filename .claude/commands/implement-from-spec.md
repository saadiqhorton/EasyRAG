---
description: Implement a feature or change by first grounding the work in spec.md and the relevant docs, then make the smallest aligned change.
---

Implement from spec: $ARGUMENTS

Required workflow:
1. Identify the governing sections in `spec.md` and `docs/`.
2. Summarize the constraints that matter.
3. Inspect the existing repo state.
4. Implement the smallest change that moves the repo toward the spec.
5. Verify the result.
6. Update docs if the behavior or structure changed.
